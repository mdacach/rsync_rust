This branch has a working version of algorithm that uses JSON format
for signature and delta files, for development purposes. The generated files
turn out much bigger (in bytes), but are human-readable.